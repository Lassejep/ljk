// Initialize Socket.IO
const socket = io();

class WebClient {
    constructor() {
        // ... (rest of provided JavaScript code) ...
    }
}

// Initialize when DOM loaded
document.addEventListener('DOMContentLoaded', () => {
    window.client = new WebClient();
});
